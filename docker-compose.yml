version: '3.8'

services:
  video-studio:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./assets:/app/assets
      - ./config:/app/config
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_SERVER_PORT=8501
    restart: unless-stopped